[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"
[project]
name = "vm_translator"
description = "VM translator for the nand2tetris course"
version = "0.1.0"
authors = [
    { name = "Adam Pajda", email = "adam_pajda@outlook.com" }
]

